# PostgreSQL Settings
POSTGRES_HOST=postgres
POSTGRES_USER=learning_user
POSTGRES_PASSWORD=verydifficlutpassword
POSTGRES_DB=learning_db
POSTGRES_PORT=5432

# Full DB URL for migrate tool and apps (used by docker-compose migrate service)
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable
LOCAL_DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable

# JWT
JWT_SECRET_KEY=your-super-secret-key-here-32-chars-min
JWT_EXPIRE_HOURS=24h
JWT_ISSUER=learning_platform
JWT_ALGO=HS256

# URLS
CONTENT_SVC_URL=http://courses-service:9000 # ПОМЕНЯТЬ ССЫЛКИ/ПОРТЫ
ASSIGNMENT_SVC_URL=http://assignment-service:8080
GATEWAY_PORT=8000